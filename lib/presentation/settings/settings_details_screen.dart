import 'package:flutter/material.dart';

class SettingsDetailsScreen extends StatelessWidget {
  const SettingsDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Settings Details',
        style: TextStyle(
            color: Colors.white
        ),
      ),
    );
  }
}
